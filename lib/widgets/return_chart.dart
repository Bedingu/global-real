import 'package:flutter/material.dart';

class ReturnChart extends StatelessWidget {
  const ReturnChart({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 200,
      alignment: Alignment.center,
      child: const Text('Gráfico aqui'),
    );
  }
}
